{
  "dependencies": {
    "nunjucks": "^3.2.4",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4",
    "yaml": "^2.4.3"
  },
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "clean": "node -e \"const fs = require('fs'); const path = require('path'); const dir = 'dist'; if (!fs.existsSync(dir)) process.exit(0); fs.readdirSync(dir).forEach(file => { const filePath = path.join(dir, file); fs.statSync(filePath).isDirectory() ? fs.rmSync(filePath, { recursive: true }) : fs.unlinkSync(filePath); }); console.log('Cleared dist directory');\"",
    "clean:build": "npm run clean && npm run build",
    "watch": "concurrently \"npx webpack --watch --config webpack.dev.js\" \"npx webpack server --config webpack.dev.js\" \"npm run watch:html\"",
    "watch:html": "concurrently \"chokidar \\\"src/html/**/**.html\\\" --command \\\"npm run clean:build\\\"\" \"chokidar \\\"src/html/**/**.php\\\" --command \\\"npm run clean:build\\\"\""
  }
}
